import os

AUTHOR = "Ubuntu"
NAME = "ubuntu_performance_misc"
CRITERIA = """
Miscellaneous Performance Regression Tests
"""
SUITE = "None"
TIME = "SHORT"
TEST_CLASS = 'kernel'
TEST_CATEGORY = 'Benchmark'
TEST_TYPE = "client"
DOC = ""

tests = [ 'kernel-wakeups',
          'userspace-wakeups',
          'interrupts',
          'context-switches',
          'forks' ]

result = job.run_test_detail(NAME, tag='setup', test_name='setup', timeout=60*20)
if result == 'GOOD':
    for test in sorted(tests):
        results = job.run_test_detail(NAME, tag=test, test_name=test, timeout=600)
        print(results)
else:
    print("ERROR: test failed to build, skipping all the sub tests")

# vi:set ts=4 sw=4 expandtab syntax=python:
