AUTHOR = "Ubuntu"
NAME = "ubuntu_performance_stream"
CRITERIA = """
Uses the stream benchmark to measure memory and compute throughput"
"""
SUITE = "None"
TIME = "SHORT"
TEST_CLASS = 'kernel'
TEST_CATEGORY = 'Benchmark'
TEST_TYPE = "client"
DOC = ""

result = job.run_test_detail(NAME, test_name='setup', tag='setup', timeout=60*30)
if result == 'GOOD':
    results = job.run_test_detail(NAME, test_name='ubuntu-performance-stream', tag='ubuntu-performance-stream', timeout=60*30) # 30 minute timeout
else:
    print("ERROR: test failed to build, skipping all the sub tests")

# vi:set ts=4 sw=4 expandtab syntax=python:
