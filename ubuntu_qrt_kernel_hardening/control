ME="SHORT"
AUTHOR = "Brad Figg <brad.figg@canonical.com>"
DOC = """
qrt is the ubuntu quality-regression-tests
"""
NAME = 'ubuntu_qrt_kernel_hardening'
TEST_CLASS = 'kernel'
TEST_CATEGORY = 'Functional'
TEST_TYPE = 'client'

result = job.run_test_detail(NAME, test_name='setup', tag='setup', timeout=60*20)
if result == 'ERROR':
    print("ERROR: test failed to build, skipping all the sub tests")
else:
    result = job.run_test_detail(NAME, test_name='test-kernel-hardening.py', tag='test-kernel-hardening.py', timeout=60*20)

# vi:set ts=4 sw=4 expandtab syntax=python:
